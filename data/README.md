Entity Sync data
==================

Esta carpeta contiene los ficheros `.yml` que el comando `drush entity:sync:import`
usa para importar entidades en Drupal.

La ruta exacta puede ajustarse desde la configuración del módulo en:
`/admin/config/entity-sync/settings`

